---
import { ArrowRight, Eye } from "lucide-react";
import LandingHeroDiagram from "./LandingHeroDiagram.astro";

interface Props {
  appUrl: string;
}

const { appUrl } = Astro.props;
---

<section class="page-container landing-hero">
  <h1 class="type-display-hero max-w-[940px]">
    <span class="landing-hero-brand">HDDN</span><br />
    Redact PDFs locally. Nothing leaves your browser.
  </h1>
  <p class="landing-hero-copy type-body-lg">
    Drop a PDF. We detect sensitive data. You approve. Export a clean,
    permanently redacted file - all inside your browser.
  </p>
  <p class="landing-hero-trust">No uploads. No servers. No risk.</p>
  <div class="landing-hero-actions">
    <a class="landing-button" href={appUrl}>
      <span>Try it now</span>
      <ArrowRight size={16} strokeWidth={1.5} />
    </a>
    <a class="landing-button-secondary" href="#how">
      <Eye size={16} strokeWidth={1.5} />
      <span>See how it works</span>
    </a>
  </div>
  <p class="landing-hero-microcopy type-caption">
    Free to try. No signup required.
  </p>

  <LandingHeroDiagram />
</section>
